# Listen address (default: :: for dual-stack IPv4+IPv6)
HOST=::

# Server port (default: 8080)
PORT=8080

# API key to protect endpoints (optional, unauthenticated if unset)
API_KEY=

# Zed credentials (optional, falls back to secret-tool on Linux)
ZED_USER_ID=
ZED_ACCESS_TOKEN=
